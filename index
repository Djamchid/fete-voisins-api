<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Proxy pour La Fête des Voisins</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        header {
            background-color: #1e5ca0;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px;
            margin-bottom: 20px;
        }
        h1 {
            margin: 0;
        }
        .status-box {
            background-color: #f8f9fa;
            border-left: 5px solid #1e5ca0;
            padding: 15px;
            margin-top: 20px;
        }
        .success {
            color: #28a745;
            font-weight: bold;
        }
        .docs {
            margin-top: 30px;
            background-color: #e6f7ff;
            padding: 20px;
            border-radius: 5px;
        }
        code {
            background-color: #f5f5f5;
            padding: 2px 5px;
            border-radius: 3px;
            font-family: 'Courier New', Courier, monospace;
        }
    </style>
</head>
<body>
    <header>
        <h1>Proxy pour La Fête des Voisins</h1>
    </header>
    
    <div class="status-box">
        <h2>Statut du service</h2>
        <p>État: <span id="status">Vérification...</span></p>
    </div>
    
    <div class="docs">
        <h2>Documentation</h2>
        <p>Ce service sert d'intermédiaire (proxy) entre votre site et l'API Google Apps Script.</p>
        <p>Il résout les problèmes CORS et permet la communication entre votre site et Google Apps Script.</p>
        
        <h3>Comment utiliser</h3>
        <p>Dans votre site principal, remplacez l'URL de l'API par :</p>
        <code>https://[votre-nom-utilisateur].github.io/fete-voisins-api/api.js</code>
        
        <p>Le proxy transmettra automatiquement les requêtes à Google Apps Script et vous renverra les réponses.</p>
    </div>
    
    <script>
        // Vérifier que le service fonctionne
        window.addEventListener('load', function() {
            const statusEl = document.getElementById('status');
            
            try {
                // Vérifier que le fichier api.js est accessible
                fetch('./api.js')
                    .then(response => {
                        if (response.ok) {
                            statusEl.textContent = 'Opérationnel';
                            statusEl.className = 'success';
                        } else {
                            statusEl.textContent = 'Erreur: Impossible d\'accéder au fichier api.js';
                            statusEl.style.color = 'red';
                        }
                    })
                    .catch(error => {
                        statusEl.textContent = 'Erreur: ' + error.message;
                        statusEl.style.color = 'red';
                    });
            } catch (error) {
                statusEl.textContent = 'Erreur: ' + error.message;
                statusEl.style.color = 'red';
            }
        });
    </script>
</body>
</html>
